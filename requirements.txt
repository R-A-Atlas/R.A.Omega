# Python: Context-Aware Research Agent (auto_bot.py) + Screen Watcher + News Scanner
# Install: pip install -r requirements.txt
yfinance>=0.2.40
pandas>=2.0.0
beautifulsoup4>=4.14.3
google-genai>=1.0.0
pydantic>=2.7.0
schedule>=1.2.0
tzdata>=2024.1
python-dotenv>=1.0.0
requests>=2.31.0
robin_stocks>=3.4.0
# Screen watcher deps (required for --watch / screen_watcher.py)
mss>=10.2.0
Pillow>=10.0.0
# Web scraper deps (required for deep_research.py — free data, no API keys)
feedparser>=6.0.0
lxml>=5.0.0
# Headless browser — unlocks JS-heavy sites (Unusual Whales, EarningsWhispers, Barchart)
# After pip install, also run: python -m playwright install chromium
playwright>=1.40.0
# Desktop notifications for real-time price alerts (alerts.py)
plyer>=2.1.0
# ── Phase 2A: Alpaca paper trading + broker APIs ──────────────────────────────
# Alpaca Developer API — zero-commission paper trading, real-time crypto/equity data
alpaca-py>=0.13.0
# chromadb — local vector DB (SEC filings + finance_knowledge); lazy init in rag_engine
chromadb>=0.4.0
# Phase 3A — ingest PDF whitepapers into finance_knowledge
pypdf>=6.11.0
# ── API server deps (api_server.py) ───────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
httpx>=0.27.0
# Supabase — multi-tenant Postgres + Auth (Phase 2)
supabase>=2.4.0
# DevSecOps / agent tests (Division 0)
pytest>=8.0.0
# Document export (api_server POST /export/*) — WeasyPrint needs GTK runtime on Windows per docs
weasyprint>=62.0
python-pptx>=0.6.23
openpyxl>=3.1.0
